export interface ProductTypeRef {
  id: string;
  name: string;
  slug: string;
}
export interface OriginRef {
  id: string;
  name: string;
  slug: string;
}
export interface ColorRef {
  id: string;
  name: string;
  hex: string | null;
}
